# Pattern Template

## Metadata

- `title`:
- `type`: `pattern`
- `tags`:
- `source`:
- `status`:
- `summary`:

## Problem

这个模式解决什么问题。

## Pattern

核心设计、流程或结构。

## Why It Works

适用原因和关键前提。

## Trade-offs

优点、缺点和边界。

## Example

一个简单示例。
